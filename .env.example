# ===========================================
# EcoLimpio Production Environment Variables
# ===========================================
# Copy this file to .env and fill in all values
# IMPORTANT: Never commit .env to version control!

# ===========================================
# DATABASE (Required)
# ===========================================
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Example for common hosting providers:
# - Neon: postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
# - Supabase: postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
# - Railway: postgresql://postgres:password@containers-xxx.railway.app:5432/railway
# - PlanetScale: mysql://user:password@aws.connect.psdb.cloud/database?sslaccept=strict
DATABASE_URL="postgresql://user:password@host:5432/ecolimpio?schema=public"

# ===========================================
# APP URL (Required for production)
# ===========================================
# Your production domain (used for cookies, redirects, CORS)
# Must include https:// protocol for production
NEXT_PUBLIC_APP_URL="https://ecolimpio.es"

# ===========================================
# GOOGLE reCAPTCHA v2 (Required)
# ===========================================
# Get your keys at: https://www.google.com/recaptcha/admin
# Choose reCAPTCHA v2 "I'm not a robot" Checkbox
# Add your production domain to the allowed domains list

# Public site key (visible in browser)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY="your-recaptcha-site-key"

# Secret key (server-side only, never expose)
RECAPTCHA_SECRET_KEY="your-recaptcha-secret-key"

# ===========================================
# SMS Service (Required for phone verification)
# ===========================================
# Bird API for sending verification codes
# Sign up at: https://bird.com/
# Setup:
# 1. Create account at https://dashboard.bird.com
# 2. Go to your workspace and create an SMS channel
# 3. Get your API access key from the dashboard
# 4. Copy the API key and paste below
#
# In development mode, SMS codes are logged to console instead of being sent
# In production, Bird API is used to send real SMS messages

BIRD_API_KEY="your-bird-api-key"

# Optional: Override the default Bird API URL (workspace/channel specific)
# BIRD_API_URL="https://api.bird.com/workspaces/YOUR_WORKSPACE_ID/channels/YOUR_CHANNEL_ID/messages"

# Note: Bird (formerly MessageBird) supports 200+ countries including Spain
# Pricing varies by destination country (verify current rates at bird.com)

# ===========================================
# OPTIONAL: Email Service (for notifications)
# ===========================================
# For booking confirmations and contact form notifications

# SMTP settings
# SMTP_HOST="smtp.example.com"
# SMTP_PORT="587"
# SMTP_USER="your-smtp-username"
# SMTP_PASSWORD="your-smtp-password"
# SMTP_FROM="noreply@ecolimpio.es"

# Or use a service like SendGrid, Resend, etc.
# SENDGRID_API_KEY="your-sendgrid-api-key"

# ===========================================
# DEPLOYMENT NOTES
# ===========================================
# 1. Run `npx prisma migrate deploy` before starting the app
# 2. Run `npx prisma generate` to generate the Prisma client
# 3. Create an admin user using: `npx ts-node scripts/create-admin.ts`
# 4. Hash-based authentication: Each session gets unique URL path (no subdomain config needed)
#    Admin panels accessible at: https://ecolimpio.es/{hash}/admin/dashboard
#    User panels accessible at: https://ecolimpio.es/{hash}/dashboard
